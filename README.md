# Gamegen Skeleton

根据 `deep-research-report.md` 的建议初始化的开发骨架：

- `packages/game-engine`: 纯逻辑对局引擎（命中判定、状态机）。
- `packages/api-contract`: API 契约与上传/差异任务端点常量。
- `apps/web`: Web MVP 入口与双图玩法基础 UI。

## Quick start

```bash
npm install
npm test
npm run build
```
