{
  "name": "gamegen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "test": "npm run --workspaces test",
    "lint": "npm run --workspaces lint"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^2.1.8"
  }
}
